# IRC Server Admin Configuration
# Lines starting with # are comments

# Main server administrator (grade 0 - highest privileges)
MAIN_ADMIN=luis-fif
ADMIN_PASSWORD=admin123

# Channel operators (grade 1) - podem usar OPER_PASSWORD
OPERATORS=kevin,maria,joao

# Voice users (grade 2) - podem falar em canais moderados
VOICE_USERS=ana,pedro,sofia

# Auto-op channels (users get operator on these channels automatically)
AUTO_OP_CHANNELS=#general,#test

# Server password for OPER command (grade 1)
OPER_PASSWORD=mypassword123

# Ban on connection (users that can't connect)
BANNED_USERS=spammer,troll

# Maximum users per channel (global setting)
MAX_USERS_DEFAULT=50