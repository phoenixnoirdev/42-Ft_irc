On va utiliser HexChat

installer:

flatpak remote-add --user --if-not-exists flathub https://flathub.org/repo/flathub.flatpakrepo

flatpak install --user io.github.Hexchat



Dans le cas ou il faudrai desintaller Hexchat pour le re-installer:

flatpak uninstall --delete-data io.github.Hexchat
